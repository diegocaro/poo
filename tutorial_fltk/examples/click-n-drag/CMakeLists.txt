CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT(click-n-drag)

FIND_PACKAGE(FLTK REQUIRED)
FIND_PACKAGE(OpenGL REQUIRED)

ADD_EXECUTABLE(click-n-drag click-n-drag.cxx)
TARGET_LINK_LIBRARIES(click-n-drag ${FLTK_LIBRARIES})
TARGET_LINK_LIBRARIES(click-n-drag ${OPENGL_LIBRARIES})